'use strict'

/**
 * The test file for <%= name %>.
 * 
 * TODO: Write tests against:
 *    - requestHandler
 *    - dbHandler
 *    - index
 */

var test = require('unit.js');;

var dbHandler = require('./<%= name %>.dbHandler');
var requestHandler = require('./<%= name %>.requestHandler');
var index = require('./index');
